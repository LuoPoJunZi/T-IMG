# Required for uploads and Telegram-hosted file access.
TG_Bot_Token=your_telegram_bot_token
TG_Chat_ID=your_telegram_chat_id

# Optional. Defaults to 20 MiB and cannot exceed that retrievable limit.
MAX_UPLOAD_SIZE_BYTES=20971520

# Required upload-page access protection. Store the first two values as
# Cloudflare Secrets, never as plaintext production variables or source code.
# Password: at least 12 characters. Session secret: at least 32 characters.
UPLOAD_ACCESS_PASSWORD=replace_with_a_strong_upload_password
UPLOAD_SESSION_SECRET=replace_with_a_long_random_session_secret

# Optional. Defaults to 604800 seconds (7 days); allowed range is 300-2592000.
UPLOAD_SESSION_MAX_AGE=604800

# Strongly recommended when the management API is enabled.
BASIC_USER=your_admin_username
BASIC_PASS=your_strong_admin_password

# Optional content moderation and whitelist mode.
ModerateContentApiKey=your_api_key
WhiteList_Mode=false

# img_url is a Cloudflare KV namespace binding, not a string environment value.
# UPLOAD_AUTH_KV is a separate required KV binding used only for login rate limits.
